version: 1.0.0
name: devtools_har
description: HAR v1.2 and browser DevTools-extended typed models for Dart.
maintainer: Roman Cinis
repository: https://github.com/tsinis/devtools_har
issue_tracker: https://github.com/tsinis/devtools_har/issues
topics:
 - http-archive
 - har
 - devtools
 - http
 - archive-http

screenshots:
  - description: 'Code quality'
    path: doc/quality.webp

environment:
  sdk: ^3.10.0

dev_dependencies:
  coverage: ^1.15.0 # From Google.
  dart_code_metrics_presets: ^2.29.0 # DCM.
  lints: ^6.1.0 # From Google.
  test: ^1.30.0 # From Google.
